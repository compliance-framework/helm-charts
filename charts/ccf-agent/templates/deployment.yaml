apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ccf-agent.fullname" . }}
  labels:
    {{- include "ccf-agent.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "ccf-agent.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "ccf-agent.selectorLabels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "ccf-agent.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      {{- with .Values.initContainers }}
      initContainers:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
      - name: {{ .Chart.Name }}
        securityContext:
          {{- toYaml .Values.securityContext | nindent 12 }}
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        command:
          - "./concom"
          - "agent"
          - "-c"
          - "/config/config.yml"
        env:
          - name: HOSTNAME
            value: {{ include "ccf-agent.hostname" . | quote }}
          {{- if .Values.agent.api.auth.enabled }}
          {{- if and .Values.agent.api.auth.clientId (or .Values.agent.api.auth.clientId.value .Values.agent.api.auth.clientId.secretKeyRef) }}
          {{- if and .Values.agent.api.auth.clientId.secretKeyRef .Values.agent.api.auth.existingSecret }}
          - name: CCF_AGENT_API_AUTH_CLIENT_ID
            valueFrom:
              secretKeyRef:
                name: {{ .Values.agent.api.auth.existingSecret }}
                key: {{ .Values.agent.api.auth.clientId.secretKeyRef }}
          {{- else if and .Values.agent.api.auth.createSecret .Values.agent.api.auth.clientId.value (not .Values.agent.api.auth.clientId.secretKeyRef) }}
          - name: CCF_AGENT_API_AUTH_CLIENT_ID
            valueFrom:
              secretKeyRef:
                name: {{ include "ccf-agent.fullname" . }}-auth
                key: CCF_AGENT_API_AUTH_CLIENT_ID
          {{- else if and .Values.agent.api.auth.clientId.value (not .Values.agent.api.auth.clientId.secretKeyRef) }}
          - name: CCF_AGENT_API_AUTH_CLIENT_ID
            value: {{ .Values.agent.api.auth.clientId.value | quote }}
          {{- end }}
          {{- end }}
          {{- if and .Values.agent.api.auth.clientSecret (or .Values.agent.api.auth.clientSecret.value .Values.agent.api.auth.clientSecret.secretKeyRef) }}
          {{- if and .Values.agent.api.auth.clientSecret.secretKeyRef .Values.agent.api.auth.existingSecret }}
          - name: CCF_AGENT_API_AUTH_CLIENT_SECRET
            valueFrom:
              secretKeyRef:
                name: {{ .Values.agent.api.auth.existingSecret }}
                key: {{ .Values.agent.api.auth.clientSecret.secretKeyRef }}
          {{- else if and .Values.agent.api.auth.createSecret .Values.agent.api.auth.clientSecret.value (not .Values.agent.api.auth.clientSecret.secretKeyRef) }}
          - name: CCF_AGENT_API_AUTH_CLIENT_SECRET
            valueFrom:
              secretKeyRef:
                name: {{ include "ccf-agent.fullname" . }}-auth
                key: CCF_AGENT_API_AUTH_CLIENT_SECRET
          {{- else if and .Values.agent.api.auth.clientSecret.value (not .Values.agent.api.auth.clientSecret.secretKeyRef) }}
          - name: CCF_AGENT_API_AUTH_CLIENT_SECRET
            value: {{ .Values.agent.api.auth.clientSecret.value | quote }}
          {{- end }}
          {{- end }}
          {{- end }}
          {{- range $secretRef := .Values.secretRefs }}
          {{- range $key := $secretRef.keys }}
          - name: {{ $key.envName }}
            valueFrom:
              secretKeyRef:
                name: {{ $secretRef.name }}
                key: {{ $key.secretKey }}
          {{- end }}
          {{- end }}
          {{- with .Values.extraEnv }}
          {{- toYaml . | nindent 10 }}
          {{- end }}
        {{- with .Values.extraEnvFrom }}
        envFrom:
          {{- toYaml . | nindent 10 }}
        {{- end }}
        resources:
          {{- toYaml .Values.resources | nindent 12 }}
        volumeMounts:
          - name: config
            mountPath: /config
            readOnly: true
          - name: ccf-tmp
            mountPath: /app/.compliance-framework
          - name: tmp
            mountPath: /tmp
          {{- with .Values.volumeMounts }}
          {{- toYaml . | nindent 10 }}
          {{- end }}
        {{- if .Values.livenessProbe.enabled }}
        livenessProbe:
          {{- toYaml (omit .Values.livenessProbe "enabled") | nindent 12 }}
        {{- end }}
        {{- if .Values.readinessProbe.enabled }}
        readinessProbe:
          {{- toYaml (omit .Values.readinessProbe "enabled") | nindent 12 }}
        {{- end }}
      volumes:
        - name: config
          configMap:
            name: {{ include "ccf-agent.fullname" . }}-config
        - name: tmp
          emptyDir: {}
        - name: ccf-tmp
          emptyDir: {}
        {{- with .Values.volumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      restartPolicy: {{ .Values.restartPolicy }}
