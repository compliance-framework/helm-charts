apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ccf-app.fullname" . }}-test-connection"
  labels:
    {{- include "ccf-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ccf-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
